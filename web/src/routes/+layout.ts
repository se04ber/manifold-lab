// Manifold Lab is a fully static, client-only app: no server routes, no
// per-request dynamic rendering, so every route can be prerendered for
// adapter-static / GitHub Pages.
export const prerender = true;
